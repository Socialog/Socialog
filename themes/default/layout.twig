{% do headlink().appendStylesheet(basePath('theme/css/main.css')) %}

{{- doctype() }}
<html>

    <head>
		{{ headTitle("Socialog").setSeparator(' - ')|raw }}
		{{ headLink() }}
		{{ headScript() }}
    </head>
    <body>

		<header class="main-header">
			<hgroup>
				<img src="{{ profile().photourl }}">
				<h1>{{ profile().name }}</h1>
                <h2>{{ profile().tagline }}</h2>
			</hgroup>
			{% include "navigation" %}
		</header>

		<section class="content">
			{{ content|raw }}
		</section>

		{{ inlineScript() }}

    </body>
</html>
